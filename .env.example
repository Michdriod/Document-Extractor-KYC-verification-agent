# Environment Variables - Example file
# Copy this file to .env and replace the values with your actual credentials

# Environment
ENVIRONMENT=development

# Server Configuration
PORT=8000
HOST=0.0.0.0

# Vision LLM credentials for OpenRouter (Day 2)
# Get your API key from https://openrouter.ai/
# This is used by pydantic-ai to connect to OpenRouter as specified in:
# https://ai.pydantic.dev/models/openai/#openrouter
OPENROUTER_API_KEY=your-api-key-here

# OpenRouter Request Headers (Required for API calls)
# During development, localhost is fine
# For production, use your actual site URL
OPENROUTER_REFERER=http://localhost:8000
OPENROUTER_TITLE=Document Extractor KYC

# Cache Settings (Day 3)
CACHE_TTL=600  # Time-to-live in seconds for cached data (10 minutes)
